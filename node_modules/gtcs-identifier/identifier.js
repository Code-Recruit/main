async function getUser(ip) {
    await fetch('https://3.69.240.204:3000/getUserFromIp/' + ip)
    .then(data => {
        return data;
    })
    .catch(error => {
        console.log('ERROR: There was a problem with the fetch operation:', error);
        return false;
    });
}

module.exports.getUser = getUser;